# Hermes environment variables (Phase 1)

# command-api, activity workers, projections
EVENT_STORE_TABLE=hermes-dev-event-store
EXECUTION_READ_MODEL_TABLE=hermes-dev-execution-read-model
EVENT_BUS_NAME=hermes-dev-events
AWS_REGION=ap-south-1

# Note: WORKFLOW_STATE_MACHINE_ARN is NOT used by command-api.
# Orchestration is triggered by EventBridge → Step Functions (native target).
# See infra/modules/eventbridge-sfn-target/
